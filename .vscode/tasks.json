{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Build and assemble the project",
      "command": "${workspaceFolder}/Build/Build.ps1",
      "args": [],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
